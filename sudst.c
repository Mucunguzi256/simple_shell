#include <stdio.h>
#include <unistd.h>

/*
 * main entry point
 * @a: int one
 * @b: omt two
 * @diff: the difference between a and b
 * Return: The diff
 */

int main(void)
{
	int a;
	int b;
	int diff = a - b;

	printf("%d/n", diff);
	return (0);
}
